let aAabout = document.querySelector("#aAabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 65) {
    aAabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 65) {
    aAabout.className = "key";
  }
});

let bBabout = document.querySelector("#bBabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 66) {
    bBabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 66) {
    bBabout.className = "key";
  }
});

let cCabout = document.querySelector("#cCabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 67) {
    cCabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 67) {
    cCabout.className = "key";
  }
});

let dDabout = document.querySelector("#dDabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 68) {
    dDabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 68) {
    dDabout.className = "key";
  }
});

let eEabout = document.querySelector("#eEabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 69) {
    eEabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 69) {
    eEabout.className = "key";
  }
});

let fFabout = document.querySelector("#fFabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 70) {
    fFabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 70) {
    fFabout.className = "key";
  }
});

let gGabout = document.querySelector("#gGabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 71) {
    gGabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 71) {
    gGabout.className = "key";
  }
});

let hHabout = document.querySelector("#hHabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 72) {
    hHabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 72) {
    hHabout.className = "key";
  }
});

let iIabout = document.querySelector("#iIabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 73) {
    iIabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 73) {
    iIabout.className = "key";
  }
});

let jJabout = document.querySelector("#jJabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 74) {
    jJabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 74) {
    jJabout.className = "key";
  }
});

let kKabout = document.querySelector("#kKabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 75) {
    kKabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 75) {
    kKabout.className = "key";
  }
});

let lLabout = document.querySelector("#lLabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 76) {
    lLabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 76) {
    lLabout.className = "key";
  }
});

let mMabout = document.querySelector("#mMabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 77) {
    mMabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 77) {
    mMabout.className = "key";
  }
});

let nNabout = document.querySelector("#nNabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 78) {
    nNabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 78) {
    nNabout.className = "key";
  }
});

let oOabout = document.querySelector("#oOabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 79) {
    oOabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 79) {
    oOabout.className = "key";
  }
});

let pPabout = document.querySelector("#pPabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 80) {
    pPabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 80) {
    pPabout.className = "key";
  }
});

let qQabout = document.querySelector("#qQabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 81) {
    qQabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 81) {
    qQabout.className = "key";
  }
});

let rRabout = document.querySelector("#rRabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 82) {
    rRabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 82) {
    rRabout.className = "key";
  }
});

let sSabout = document.querySelector("#sSabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 83) {
    sSabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 83) {
    sSabout.className = "key";
  }
});

let tTabout = document.querySelector("#tTabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 84) {
    tTabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 84) {
    tTabout.className = "key";
  }
});

let uUabout = document.querySelector("#uUabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 85) {
    uUabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 85) {
    uUabout.className = "key";
  }
});

let vVabout = document.querySelector("#vVabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 86) {
    vVabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 86) {
    vVabout.className = "key";
  }
});

let wWabout = document.querySelector("#wWabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 87) {
    wWabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 87) {
    wWabout.className = "key";
  }
});

let xXabout = document.querySelector("#xXabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 88) {
    xXabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 88) {
    xXabout.className = "key";
  }
});

let yYabout = document.querySelector("#yYabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 89) {
    yYabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 89) {
    yYabout.className = "key";
  }
});

let zZabout = document.querySelector("#zZabout");

window.addEventListener("keydown", function (event) {
  if (event.keyCode === 90) {
    zZabout.className = "key-pressed";
  }
});

window.addEventListener("keyup", function (event) {
  if (event.keyCode === 90) {
    zZabout.className = "key";
  }
});

let dotAbout = document.querySelector("#dotAbout");

window.addEventListener("keydown", function (event) {
    if (event.keyCode === 190) {
      dotAbout.className = "key-pressed";
    }
  });
  
  window.addEventListener("keyup", function (event) {
    if (event.keyCode === 190) {
      dotAbout.className = "key";
    }
  });
  
let commaAbout = document.querySelector("#commaAbout");

window.addEventListener("keydown", function (event) {
    if (event.keyCode === 188) {
      commaAbout.className = "key-pressed";
    }
  });
  
  window.addEventListener("keyup", function (event) {
    if (event.keyCode === 188) {
      commaAbout.className = "key";
    }
  });
  
let semicolonAbout = document.querySelector("#semicolonAbout");

window.addEventListener("keydown", function (event) {
    if (event.keyCode === 186) {
      semicolonAbout.className = "key-pressed";
    }
  });
  
  window.addEventListener("keyup", function (event) {
    if (event.keyCode === 186) {
      semicolonAbout.className = "key";
    }
  });
  
let backspaceAbout = document.querySelector("#backspaceAbout");

window.addEventListener("keydown", function (event) {
    if (event.keyCode === 8) {
      backspaceAbout.className = "key-pressed";
    }
  });
  
  window.addEventListener("keyup", function (event) {
    if (event.keyCode === 8) {
      backspaceAbout.className = "key";
    }
  });
  
 
let spacebarAbout = document.querySelector("#spacebarAbout");

window.addEventListener("keydown", function (event) {
    if (event.keyCode === 32) {
      spacebarAbout.className = "key-pressed";
    }
  });
  
  window.addEventListener("keyup", function (event) {
    if (event.keyCode === 32) {
      spacebarAbout.className = "key";
    }
  });
   